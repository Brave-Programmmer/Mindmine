---
export const prerender = false;
import Layout from "../../layouts/Layout.astro";
import FetchBook from "../../components/FetchBook";
const { params } = Astro;
const id = params.id;
---

<Layout title={`Reading Book`}>
  <FetchBook id={id} client:only="react" />
</Layout>
